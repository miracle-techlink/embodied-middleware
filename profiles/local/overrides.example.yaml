# 本机覆盖样例;复制为 profiles/local/overrides.yaml(该文件不入仓)。
devices:
  wrist_cam:
    params:
      serial: CV2856D0006R
  front_cam:
    params:
      device: /dev/video0
record:
  dataset_root: ~/.cache/huggingface/lerobot
secrets:
  hf_token_env: HF_TOKEN
